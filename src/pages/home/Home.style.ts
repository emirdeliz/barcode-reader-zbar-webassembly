import styled, { css } from 'styled-components';

export const Title = styled.title``;

export const TitleSection = styled.h1``;

export const Main = styled.main``;

export const InputUpload = styled.input.attrs({ type: 'file' })`
	width: 100%;
`;